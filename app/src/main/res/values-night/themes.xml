<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PertoDeMim" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/primary_teal</item>
        <item name="colorSecondary">@color/primary_orange</item>
        <item name="android:statusBarColor">@color/header_teal</item>
        <item name="colorSecondaryContainer">@android:color/transparent</item>
    </style>
</resources>